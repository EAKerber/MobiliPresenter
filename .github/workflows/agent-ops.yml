name: Agent Ops
on:
  pull_request:
    paths: ['AGENTS.md','ops/**','tools/**','docs/plans/**','.github/workflows/agent-ops.yml']
  push:
    branches: ['renderer/**','architecture/**','ops/**']
    paths: ['AGENTS.md','ops/**','tools/**','docs/plans/**','.github/workflows/agent-ops.yml']
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      pull-requests: read
      checks: read
    steps:
      - name: Checkout branch head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Toolbox unit tests
        run: python -m unittest discover -s tools/tests -p 'test_*.py' -v
      - name: Semantic contracts
        run: python -m tools.semantics check --json
      - name: Verify continuation state
        run: python tools/continuation.py verify --json
      - name: Capability lifecycle guard
        if: github.event_name == 'pull_request'
        run: python tools/capability_gates_ci.py --base-sha "${{ github.event.pull_request.base.sha }}" --head-sha "${{ github.event.pull_request.head.sha }}" --json
      - name: Doctor
        run: python tools/agent.py doctor --json
      - name: Verify local coherence
        run: python tools/agent.py verify --json
      - name: Verify remote PR identity
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: python tools/agent.py verify --remote --json
      - name: Generate branch prune audit
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/prune_plan.py --json > /tmp/git-prune-plan.json
          python -m json.tool /tmp/git-prune-plan.json > /dev/null
          python - <<'PY'
          import json
          p=json.load(open('/tmp/git-prune-plan.json'))
          assert p['schemaVersion'] == 'GitPrunePlan 0.2'
          assert p['destructiveApplySupported'] is False
          assert all(e['autoDeleteEligible'] for e in p['entries'] if e['action'] == 'delete-candidate')
          assert all(e['action'] == 'keep' for e in p['entries'] if e['protections'])
          PY
      - name: Upload branch prune audit
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: git-prune-plan
          path: /tmp/git-prune-plan.json
          if-no-files-found: error
      - name: Generate Project Machine inspection
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/project_machine.py inspect --live --json > /tmp/project-machine-inspection.json
          python -m json.tool /tmp/project-machine-inspection.json > /dev/null
          python tools/project_machine.py validate /tmp/project-machine-inspection.json --json
      - name: Upload Project Machine inspection
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: project-machine-inspection
          path: /tmp/project-machine-inspection.json
          if-no-files-found: error
      - name: Generate maintenance inspection
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/maintenance_inspect.py --remote --json > /tmp/maintenance-inspection.json
          python -m json.tool /tmp/maintenance-inspection.json > /dev/null
      - name: Upload maintenance inspection
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: maintenance-inspection
          path: /tmp/maintenance-inspection.json
          if-no-files-found: error
      - name: Generate live supervisor inspection
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/maintenance_live.py --json > /tmp/live-supervisor-inspection.json
          python -m json.tool /tmp/live-supervisor-inspection.json > /dev/null
      - name: Upload live supervisor inspection
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: live-supervisor-inspection
          path: /tmp/live-supervisor-inspection.json
          if-no-files-found: error
      - name: Generate Scheduler plan
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/scheduler_plan.py --live --json > /tmp/scheduler-plan.json
          python -m json.tool /tmp/scheduler-plan.json > /dev/null
          python - <<'PY'
          import json
          p=json.load(open('/tmp/scheduler-plan.json'))
          assert p['readOnly'] is True
          assert p['transportSideEffects'] is False
          assert p['semanticAuthority'] is False
          assert p['dispatch']['channelClass'] in {'worker','supervisor','human','none'}
          PY
      - name: Upload Scheduler plan
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: scheduler-plan
          path: /tmp/scheduler-plan.json
          if-no-files-found: error
      - name: Generate integration reconcile evidence
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          python tools/integration_reconcile.py reconcile-plan "$PR_NUMBER" --json > /tmp/integration-reconcile-plan.json
          python -m json.tool /tmp/integration-reconcile-plan.json > /dev/null
      - name: Upload integration reconcile evidence
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: integration-reconcile-plan
          path: /tmp/integration-reconcile-plan.json
          if-no-files-found: error
      - name: Generate handoff evidence
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python tools/agent.py handoff --json > /tmp/agent-handoff.json
          python -m json.tool /tmp/agent-handoff.json > /dev/null
      - name: Upload handoff evidence
        uses: actions/upload-artifact@v4
        with:
          name: agent-ops-handoff
          path: /tmp/agent-handoff.json
          if-no-files-found: error
